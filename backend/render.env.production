# ============================================================================
# RENDER PRODUCTION ENVIRONMENT VARIABLES
# ============================================================================
# Copy these values to your Render Dashboard → Settings → Environment Variables
# 
# IMPORTANT: 
# - NEVER commit this file with real secrets to git
# - Use Render Dashboard to set environment variables (Settings → Environment)
# - This is a TEMPLATE - replace placeholder values with your actual values
# ============================================================================

# ----------------------------------------------------------------------------
# 🔒 REQUIRED - FLASK CONFIGURATION
# ----------------------------------------------------------------------------
FLASK_ENV=production
PORT=10000

# SECRET_KEY - Generate a new one for production:
# Method 1 (Node.js): node -e "const crypto = require('crypto'); console.log(crypto.randomBytes(32).toString('hex'))"
# Method 2 (Anaconda): /opt/anaconda3/bin/python -c "import secrets; print(secrets.token_hex(32))"
# Method 3 (Online): https://randomkeygen.com/
# 
# Generated production key (ready to use):
SECRET_KEY=4a6be0d4857b4f0816eb617d7d2aaf38065db78c21622bec5ac966bee0fa1d34

# ----------------------------------------------------------------------------
# 🌐 REQUIRED - CORS CONFIGURATION (Frontend URL)
# ----------------------------------------------------------------------------
# Add your Vercel frontend URL - this allows your frontend to make API calls
# Multiple origins: comma-separated (no spaces)
CORS_ORIGINS=https://lexsy-web-app.vercel.app,https://www.lexsy-web-app.vercel.app

# Optional: Allow all origins (NOT RECOMMENDED for production)
# CORS_ALLOW_ALL=false

# ----------------------------------------------------------------------------
# 🤖 REQUIRED - AI SERVICE (Groq API)
# ----------------------------------------------------------------------------
# Get your API key from: https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here

# Optional: Specify Groq model (default: openai/gpt-oss-120b)
# GROQ_MODEL=openai/gpt-oss-120b

# ----------------------------------------------------------------------------
# 📁 FILE UPLOAD CONFIGURATION (Optional - has defaults)
# ----------------------------------------------------------------------------
# Maximum file upload size in bytes (10MB = 10485760)
MAX_FILE_SIZE=10485760

# Upload directories (relative to backend/ root)
UPLOAD_FOLDER=uploads
PROCESSED_FOLDER=processed

# ----------------------------------------------------------------------------
# 🔐 FIREBASE AUTHENTICATION (Optional but Recommended)
# ----------------------------------------------------------------------------
# Firebase uses firebase-service-account.json file
# Upload this file to Render via:
# Render Dashboard → Settings → Environment → Add Environment File
# File name: firebase-service-account.json
# OR set the path if using environment variable:
# FIREBASE_SERVICE_ACCOUNT=/opt/render/project/src/backend/firebase-service-account.json

# Note: firebase-service-account.json must be uploaded separately to Render
# It's not set as an environment variable, but as a file

# ----------------------------------------------------------------------------
# 💾 REDIS SESSION MANAGEMENT (Optional - Fallback to in-memory if not set)
# ----------------------------------------------------------------------------
# If using Render Redis addon:
# REDIS_URL=redis://red-xxxxx:6379

# OR configure manually:
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=your_redis_password

# Session timeout in hours (default: 168 = 7 days)
# SESSION_TIMEOUT_HOURS=168

# ----------------------------------------------------------------------------
# 🚀 RENDER-SPECIFIC NOTES
# ----------------------------------------------------------------------------
# 1. PORT is automatically set by Render (usually 10000 for free tier)
# 2. All environment variables must be set in Render Dashboard
# 3. Go to: Your Service → Settings → Environment → Add Environment Variable
# 4. For secrets, mark as "Secret" in Render
# 5. After adding variables, redeploy your service

# ============================================================================
# ✅ QUICK SETUP CHECKLIST
# ============================================================================
# [ ] FLASK_ENV=production
# [ ] SECRET_KEY (generate new one!)
# [ ] CORS_ORIGINS (your Vercel URL)
# [ ] GROQ_API_KEY (from Groq console)
# [ ] firebase-service-account.json (upload as file, not env var)
# [ ] PORT=10000 (optional, Render sets this automatically)
# [ ] Optional: Redis configuration
# ============================================================================

